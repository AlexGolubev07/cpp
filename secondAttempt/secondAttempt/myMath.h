#pragma once

namespace myMath
{
	// calculate factorial
	int factorial(int number);

	// description
	int sum(int number);

	// max
	int max(int number1, int number2);

	// max
	int max(int number1, int number2, int number3);
}

